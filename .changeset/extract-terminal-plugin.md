---
"@jmfederico/pi-web": patch
---

Ship Terminal as a required bundled browser/server plugin while preserving its panel, navigation, PTY, full replay, reconnect, command-run, and selected-machine workflows. Add revision-paired, workspace-scoped plugin requests and bounded duplex channels with ordered input/output drain, hard physical teardown, and explicit authoritative safe-start recovery when the required Terminal entry cannot activate.
